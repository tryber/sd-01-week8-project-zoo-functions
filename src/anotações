let; 
const; x 
arrow functions; x 
template literals; x
spread operator; 
parâmetro rest;
object destructuring; x
array destructuring;
default destructuring;
abbreviation object literal; x
default params;
higher order functions; x
classes;
atributos de classes;
métodos de instâncias de classes;
métodos estáticos de classes.